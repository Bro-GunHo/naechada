import React, { Component, useEffect, useState, useRef  } from 'react';
import { createAppContainer, NavigationEvents } from 'react-navigation';
import { View, Text, Image, PermissionsAndroid, StyleSheet, TouchableOpacity, Alert, Linking, BackHandler, ActivityIndicator, StatusBar, NativeModules  } from 'react-native';
import { NavigationContainer, StackActions, useFocusEffect } from '@react-navigation/native';

import { Container, Content } from 'native-base';

function Test() {

    return (<View>
        <Text>ㅁㅁ</Text>
    </View>);

}